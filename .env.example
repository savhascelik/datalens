# Frontend only sends schema metadata and the user prompt to this endpoint.
# Implement this API server-side so the LLM key is never exposed in the browser.
VITE_LLM_API_URL=http://localhost:3001/api/analysis
